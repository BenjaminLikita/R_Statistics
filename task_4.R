#Generate sample set
samples <- runif(20)

#Calculate the mean
mean_value <- mean(samples)

#Calculate the Variance
variance_value <- var(samples)


#Print the results
cat("Mean: ", mean_value, "\n")
cat("Variance: ", variance_value, "\n")
